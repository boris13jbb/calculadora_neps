{
  "name": "firestore-rules-tests",
  "private": true,
  "description": "Pruebas de Firestore Rules (emulador) — sin deploy",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test parametric_roles.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
